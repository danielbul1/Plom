[project]
name = "plom"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Pact Dev", email = "danielbul2006@gmail.com" }
]
requires-python = ">=3.14"
dependencies = [
    "fastapi>=0.141.1",
    "numpy>=2",
    "uvicorn>=0.53.0",
    "websockets>=17.1",
]

[project.scripts]
plom = "plom.cli:main"

[build-system]
requires = ["uv_build>=0.12.6,<0.13.0"]
build-backend = "uv_build"

[dependency-groups]
dev = [
    "httpx>=0.28.1",
    "pytest>=9.1.1",
]
